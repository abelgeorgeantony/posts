source "https://rubygems.org"

# Use the GitHub Pages gem to keep your local environment 
# identical to the GitHub Pages build servers.
gem "github-pages", group: :jekyll_plugins

# Required to pull in abelgeorgeantony.github.io as the theme
gem "jekyll-remote-theme"